# Render.com Environment Variables Template
# Copy these to your Render Dashboard → Environment Variables

# Database (Render provides this automatically if you create a PostgreSQL database)
DATABASE_URL=postgresql://user:password@hostname:port/database

# JWT Secret (use "Generate Value" in Render dashboard)
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production

# Server Configuration
NODE_ENV=production
PORT=10000

# Frontend URL (update after deploying frontend)
FRONTEND_URL=https://your-frontend.vercel.app
FRONTEND_ORIGIN=https://your-frontend.vercel.app

# Email Configuration (Optional - for email features)
GMAIL_CLIENT_ID=your-gmail-client-id
GMAIL_CLIENT_SECRET=your-gmail-client-secret
GMAIL_REDIRECT_URI=https://your-backend.onrender.com/api/gmail/callback

OUTLOOK_CLIENT_ID=your-outlook-client-id
OUTLOOK_CLIENT_SECRET=your-outlook-client-secret
OUTLOOK_REDIRECT_URI=https://your-backend.onrender.com/api/outlook/callback

# Email Encryption Key (32 characters)
EMAIL_ENCRYPTION_KEY=your-32-character-encryption-key

# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Google Calendar (Optional)
GOOGLE_CALENDAR_CLIENT_ID=your-google-calendar-client-id
GOOGLE_CALENDAR_CLIENT_SECRET=your-google-calendar-client-secret
GOOGLE_CALENDAR_REDIRECT_URI=https://your-backend.onrender.com/api/google-calendar/callback
